[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scarlet"
version = "0.2.0"
description = "NeXus-native, instrument-agnostic framework for SANS data reduction."
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [{ name = "SCARLET Contributors" }]
dependencies = [
    "PySide6>=6.7",
    "PyYAML>=6.0",
    "h5py>=3.10",
    "jupyterlab>=4",
    "matplotlib>=3.8",
    "numpy>=1.26,<2; python_version < '3.10'",
    "numpy>=1.26; python_version >= '3.10'",
    "pandas>=2.2",
    "scipy>=1.12",
    "scipp>=23.8,<24; python_version < '3.10'",
    "scipp>=25.0; python_version >= '3.10'",
    "silx>=2.0",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
scarlet = "scarlet.cli:main"
viewer = "scarlet.gui.scarlet_viewer:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
include = ["scarlet*"]

[tool.setuptools.package-data]
"scarlet.schemas" = ["*.yaml", "*.yml", "README.md"]
